export interface MasterWork {
  id: number;        // jedinstveni ID rada
  masterId: number;  // ID majstora kome pripada rad
  title: string;     // naslov rada
  image: string;     // URL slike rada
}